fastapi>=0.111.0
starlette>=0.37.2
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
websockets>=12.0
sqlalchemy>=2.0.30
groq>=0.9.0
httpx<0.28.0
python-dotenv>=1.0.1
pydantic>=2.9.0
numpy
Pillow
google-genai
edge-tts
pydub
# ── Optional: Object Detection (local dev only) ──────
# These packages pull in PyTorch (~2GB) and will fail
# on memory-constrained hosts like Render free tier.
# Install locally with: pip install -r requirements-ml.txt
# The detection route handles missing imports gracefully.
